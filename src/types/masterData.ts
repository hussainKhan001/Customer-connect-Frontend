export interface Project {
  code: string;
  name: string;
  entity: string;
  launch: number | null;
  lr: number | null;
  ask: number | null;
  resale: number | null;
  circle: number | null;
  noted: string | null;
  by: string | null;
  basis: string | null;
}

export interface Occupation {
  k: string;
  b: number;
  band: string | null;
}
